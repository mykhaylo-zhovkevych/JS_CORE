//      `` (backticks)

// Einfache Verwendung
const name = "Alice";
const greeting = `Hello, ${name}!`; // Verwendung der Template-Syntax
console.log(greeting); // Ausgabe: Hello, Alice!

// Mehrzeilige Strings
const multilineString = `This is a st


ring
that spans multiple lines.`;
console.log(multilineString);

// Ausdrucksberechnungen
const a = 5;
const b = 10;
const result = `The sum of ${a} and ${b} is ${a + b}.`;
console.log(result); // Ausgabe: The sum of 5 and 10 is 15.

/*

// Beispiel für einfache Verwendung:

const name = 'Alice';
const greeting = 'Hello, ' + name + '!';
console.log(greeting); // Ausgabe: Hello, Alice!


// Beispiel für mehrzeilige Strings:

const multilineString = 'This is a string\n' +
                        'that spans multiple lines.';
console.log(multilineString);


// Beispiel für einfache Verwendung

const a = 5;
const b = 10;
const result = 'The sum of ' + a + ' and ' + b + ' is ' + (a + b) + '.';
console.log(result); // Ausgabe: The sum of 5 and 10 is 15.



*/
